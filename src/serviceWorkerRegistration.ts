export const fetchTabContent = (tabId:string) => {
    // chrome.tabs.get(tabId, (tab) => {
    //   if (tab) {
    //     console.log(tab.url); // Fetch tab URL
    //     console.log(tab.title); // Fetch tab title
    //     // You can further process the tab content here
    //   }
    // });
  };